## 启动 draw.io 项目

draw.io 是一个**纯前端项目**（没有 `package.json`，不需要 `npm install` 或构建步骤），只需要一个**静态文件服务器**来托管 `src/main/webapp/` 目录即可。以下是几种启动方式：

---

### 方式一：使用 Python 内置 HTTP 服务器（最简单）

```powershell
cd d:\00-sourcecode\GitHub\drawio\src\main\webapp
python -m http.server 8080
```

然后在浏览器中打开：**http://localhost:8080/index.html**

---

### 方式二：使用 Node.js 的 `http-server`

```powershell
npx http-server d:\00-sourcecode\GitHub\drawio\src\main\webapp -p 8080
```

然后在浏览器中打开：**http://localhost:8080/index.html**

---

### 方式三：使用 VS Code 的 Live Server 插件

1. 在 VS Code 中安装 **Live Server** 插件
2. 右键点击 `src/main/webapp/index.html`
3. 选择 **"Open with Live Server"**

---

### 方式四：使用 Docker（官方镜像）

```powershell
docker run -it -p 8080:8080 --name drawio jgraph/drawio
```

然后访问：**http://localhost:8080**

---

### 方式五：直接打开文件（有限制）

理论上可以直接双击打开 `d:\00-sourcecode\GitHub\drawio\src\main\webapp\index.html`，但由于浏览器的安全策略（CORS），部分功能（如加载外部资源）可能无法正常工作，**推荐使用静态服务器**。

---

### 关键目录结构

| 路径 | 说明 |
|------|------|
| `src/main/webapp/index.html` | 主入口页面 |
| `src/main/webapp/js/` | JavaScript 源码 |
| `src/main/webapp/styles/` | CSS 样式 |
| `src/main/webapp/images/` | 图片资源 |
| `src/main/webapp/stencils/` | 图形模板 |
| `src/main/webapp/plugins/` | 插件 |
| `src/main/webapp/resources/` | 国际化资源 |

> **推荐**：如果你本地有 Python，方式一最快；如果有 Node.js，方式二最方便。入口文件就是 `src/main/webapp/index.html`。